name: Ping Render App

on:
  schedule:
    - cron: "*/8 9-23 * * *"
    - cron: "*/8 0-2 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl https://tascadoalfredo.onrender.com/