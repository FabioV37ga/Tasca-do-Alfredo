name: Ping Render App

on:
  schedule:
    - cron: "*/10 9-23 * * *"
    - cron: "*/10 0-2 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl https://tascadoalfredo.onrender.com/