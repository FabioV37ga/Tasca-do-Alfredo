name: Keep Render Awake

on:
  schedule:
    # Every 5 minutes, from 07:00 to 21:55 (Brazil/São Paulo UTC-3)
    - cron: "*/5 10-23 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: curl -I https://tascadoalfredo.onrender.com/